# PostgreSQL connection used by the API, worker, and migrations.
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/parallel_usdp

# Optional separate database used by integration tests.
TEST_DATABASE_URL=postgresql://postgres:postgres@localhost:5432/parallel_usdp_test

# HyperEVM mainnet RPC. Never commit a provider key.
HYPEREVM_RPC_URL=https://rpc.example.invalid

# Optional state-only RPCs for the other official sUSDp chains. Missing URLs
# leave that chain explicitly unavailable without breaking HyperEVM.
ETHEREUM_RPC_URL=https://ethereum-rpc.publicnode.com
BASE_RPC_URL=https://mainnet.base.org
SONIC_RPC_URL=https://rpc.soniclabs.com
AVALANCHE_RPC_URL=https://api.avax.network/ext/bc/C/rpc

# Optional free-tier Alchemy key used as a recent-state/log RPC fallback.
ALCHEMY_API_KEY=

# Optional free-tier OnFinality key required for full pinned archive discovery.
ONFINALITY_API_KEY=

# Index only blocks at or below head - FINALITY_LAG.
FINALITY_LAG=5

# Starting log range; the adaptive planner may reduce it.
RPC_LOG_CHUNK_SIZE=50

# Pace historical RPC starts conservatively; 1500 ms is about 40/minute.
RPC_REQUEST_INTERVAL_MS=1500

# MVP price policy is finalized during protocol discovery.
PRICE_SOURCE=unconfigured

# Finalized state/YPO refresh target in seconds.
REFRESH_INTERVAL_SECONDS=30

# Maximum age for a component to qualify for a complete global snapshot.
GLOBAL_SNAPSHOT_MAX_AGE_SECONDS=1800

# Opt in to tests that call the configured RPC.
RUN_NETWORK_TESTS=0

# Start the resumable seven-day worker with the deployed web service.
RUN_SEVEN_DAY_BACKFILL=0

# Periodically capture current state for all configured sUSDp chains.
RUN_MULTICHAIN_SNAPSHOTS=0
